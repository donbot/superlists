description "Gunicorn server for SITENAME"

start on net-device-up
stop on shutdown

restart

setuid donbot
chdir /home/donbot/sites/SITENAME/source

exec ../virtualenv/bin/gunicorn \
    --bind unix:/tmp/SITENAME.socket \
    superlist.wsgi:application
